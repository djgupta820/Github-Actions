name: Hello World
on: push
jobs:
  greet:
    name: greet the user
    runs-on: ubuntu-latest

    steps:
      - name: Print Greeting
        run: echo "Hello Github Actions!"

      - name: Print Date
        run: date

      - name: Print Working Directory
        run: pwd

      - name: List Files
        run: ls -la